import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Placeholder for future Next.js config; keep minimal to avoid
  // invalid experimental flags while v2 work is in progress.
};

export default nextConfig;
